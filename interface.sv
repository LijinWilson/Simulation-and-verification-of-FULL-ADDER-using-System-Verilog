interface intf;
  logic a;
  logic b;
  logic c;
  logic sum;
  logic carry;
  
  // Clocking block
  // modport
  
  // Whatever signal are signal are going inside the DUT we have to mentioned all inside interface
  // as it is an combinational circuit we don't need these.
endinterface
